<div class="container-fluid">
    <div class="row-fluid">

        <div class="span10">
            <!--Body content-->
            <ul class="lists">
                <li ng-repeat="list in user.toDoLists"
                    class="thumbnail list-listing">
                    <a href="#/lists/{{list.id}}">{{list.title}}</a>
                </li>
            </ul>
        </div>

    </div>
</div>